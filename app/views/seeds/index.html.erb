<% content_for(:title_tag) do %>
  Bienvenue sur Seedoak
<% end %>

<div class="page-container">
  <div class="seed-feed">


    <div class="categories hidden-xs">

      <div class="filter-title">
        <p class="categories-header">SEEDS</p>
        <div class="sub-categories">
          <p class="sub-title"><span class="filter-icon glyphicon glyphicon-globe"></span>Tous</p>
          <p class="sub-title"><span class="filter-icon glyphicon glyphicon-fire"></span>Tendance</p>
          <p class="sub-title"><span class="filter-icon glyphicon glyphicon-leaf"></span>Récents</p>
          <p class="sub-title"><span class="filter-icon glyphicon glyphicon-flash"></span>Dernière chance</p>
          <p class="sub-title"><span class="filter-icon glyphicon glyphicon-random"></span>Hasard</p>
        </div>
      </div>

      <div class="filter-title">
        <p class="categories-header">CATEGORIES</p>
        <div class="sub-categories">
          <% @categories.each do |category| %>
            <div class="sub-title <%= category.id %>">
              <p><%= image_tag "categories.jpg", class: "categories-icon" %>
              </p>
              <p><%= link_to category.name, seeds_path(category: category.id), remote: true %>
              </p>
            </div>
          <% end %>
        </div>
      </div>
    </div>

    <div class="seed-presentation">
      <%= render @seeds, seeds: @seeds%>
      <%= render @seeds, seeds: @seeds%>
      <%= render @seeds, seeds: @seeds%>
    </div>

  </div>
</div>




