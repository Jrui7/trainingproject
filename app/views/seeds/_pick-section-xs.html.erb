
<% if current_user %>
  <% if current_user.has_picked?(@seed) %>
    <div class="picked-bar-xs hidden-md hidden-lg">
      <p class="has_picked">Mon pick : <%= Pick.my_pick(current_user, @seed).first.price%> €</p>
    </div>
  <% else %>
    <%= link_to new_seed_pick_path(@seed), class: "xs-pick" do%>
      <div class="pick-bar-xs hidden-md hidden-lg">
        <i class="fa fa-hand-lizard" aria-hidden="true"> </i>
        <p>Pick it</p>
      </div>
    <% end %>
  <% end %>
<% else %>
  <%= link_to new_user_session_path do %>
    <div class="pick-bar-xs hidden-md hidden-lg">
      <i class="fa fa-hand-lizard" aria-hidden="true"> </i>
      <p>Pick it</p>
    </div>
  <% end %>
<% end %>
